{
  "name": "@trigo/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "gerar-tipos": "openapi-typescript openapi.json -o src/gerado/api.ts",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@trigo/core": "workspace:*"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.4",
    "typescript": "^5.9.2"
  }
}
